<?xml version="1.0"?>
<ruleset name="PHP Compatibility Check">
    <description>Check PHP 8.5 compatibility</description>

    <!-- Test against PHP 7.4 to 8.5 -->
    <config name="testVersion" value="7.4-8.5"/>

    <!-- Scan these directories -->
    <file>src/</file>
    <file>tests/</file>

    <!-- Exclude vendor and cache directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/tmp/*</exclude-pattern>
    <exclude-pattern>*/cache/*</exclude-pattern>

    <!-- Use PHPCompatibility ruleset -->
    <rule ref="PHPCompatibility"/>

    <!-- Show progress -->
    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="extensions" value="php"/>
</ruleset>
